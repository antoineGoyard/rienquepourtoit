window.onload = () => {
    // Gestion des boutons "Supprimer"
    let upload = document.getElementById('ad_picture')
    upload.addEventListener("change", function(e){
        // On empêche la navigation
        
    })
}