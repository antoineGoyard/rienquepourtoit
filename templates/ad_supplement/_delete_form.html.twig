<form method="post" action="{{ path('ad_supplement_delete', {'id': ad_supplement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ad_supplement.id) }}">
    <button class="btn">Delete</button>
</form>
